/*

undefined
MaaS customer virtual cards add token reference response

!!! AUTO GENERATED BY IOTSFJS REFRAIN FROM MANUAL EDITING !!!
See https://www.npmjs.com/package/io-ts-from-json-schema

*/

import * as t from 'io-ts';
import * as VirtualCard_ from '../virtualCard';

type Defined =
  | Record<string, unknown>
  | Array<unknown>
  | string
  | boolean
  | number
  | null;
const Defined = t.union([
  t.UnknownRecord,
  t.UnknownArray,
  t.string,
  t.boolean,
  t.number,
  t.null,
]);

export const schemaId =
  'http://maasglobal.com/maas-backend/customers/virtual-cards/add-token-reference/response.json';

// Response
// The default export. More information at the top.
export type Response = t.Branded<
  {
    virtualCard?: VirtualCard_.VirtualCard;
  } & {
    virtualCard: Defined;
  },
  ResponseBrand
>;
export const Response = t.brand(
  t.intersection([
    t.partial({
      virtualCard: VirtualCard_.VirtualCard,
    }),
    t.type({
      virtualCard: Defined,
    }),
  ]),
  (
    x,
  ): x is t.Branded<
    {
      virtualCard?: VirtualCard_.VirtualCard;
    } & {
      virtualCard: Defined;
    },
    ResponseBrand
  > => true,
  'Response',
);
export interface ResponseBrand {
  readonly Response: unique symbol;
}

export default Response;

// Success
