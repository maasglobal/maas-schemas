/*

undefined
MaaS tsp package schema

!!! AUTO GENERATED BY IOTSFJS REFRAIN FROM MANUAL EDITING !!!
See https://www.npmjs.com/package/io-ts-from-json-schema

*/

import * as t from 'io-ts';
import { NonEmptyArray } from 'fp-ts/lib/NonEmptyArray';
import { nonEmptyArray } from 'io-ts-types/lib/nonEmptyArray';

export const schemaId = 'http://maasglobal.com/core/package.json';

// Package
// The default export. More information at the top.
export type Package = t.Branded<
  {
    id?: string;
  },
  PackageBrand
>;
export type PackageC = t.BrandC<
  t.PartialC<{
    id: t.StringC;
  }>,
  PackageBrand
>;
export const Package: PackageC = t.brand(
  t.partial({
    id: t.string,
  }),
  (
    x,
  ): x is t.Branded<
    {
      id?: string;
    },
    PackageBrand
  > => true,
  'Package',
);
export interface PackageBrand {
  readonly Package: unique symbol;
}
/** require('io-ts-validator').validator(nonEmptyArray(Package)).decodeSync(examplesPackage) // => examplesPackage */
export const examplesPackage: NonEmptyArray<Package> = ([
  { id: 'package-id' },
] as unknown) as NonEmptyArray<Package>;

export default Package;

// Success
