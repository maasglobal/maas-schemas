/*

undefined
Experimental synopses array

!!! AUTO GENERATED BY IOTSFJS REFRAIN FROM MANUAL EDITING !!!
See https://www.npmjs.com/package/io-ts-from-json-schema

*/

import * as t from 'io-ts';
import * as Synopsis_ from './synopsis';
import { NonEmptyArray } from 'fp-ts/lib/NonEmptyArray';
import { nonEmptyArray } from 'io-ts-types/lib/nonEmptyArray';

export const schemaId = 'https://schemas.maas.global/environments/synopses.json';

// Synopses
// The default export. More information at the top.
export type Synopses = t.Branded<Array<Synopsis_.Synopsis>, SynopsesBrand>;
export type SynopsesC = t.BrandC<t.ArrayC<typeof Synopsis_.Synopsis>, SynopsesBrand>;
export const Synopses: SynopsesC = t.brand(
  t.array(Synopsis_.Synopsis),
  (x): x is t.Branded<Array<Synopsis_.Synopsis>, SynopsesBrand> => true,
  'Synopses',
);
export interface SynopsesBrand {
  readonly Synopses: unique symbol;
}
/** require('io-ts-validator').validator(nonEmptyArray(Synopses)).decodeSync(examplesSynopses) // => examplesSynopses */
export const examplesSynopses: NonEmptyArray<Synopses> = ([
  [
    {
      environment: {
        id: 'production',
        live: true,
        account: 'production',
        description: 'Production environment',
      },
      group: { name: 'Core Environments' },
      account: {
        id: '001234567890',
        name: 'Example Account',
        description: 'This account is but an example account',
      },
    },
    {
      environment: {
        id: 'testing',
        live: false,
        account: 'testing',
        description: 'Testing environment',
      },
      group: { name: 'Core Environments' },
      account: { id: '101234567890' },
    },
    {
      environment: {
        id: 'fantasy-topping',
        live: false,
        account: 'testing',
        name: 'Fantasy Topping',
        description: 'Add support for pizza customization',
      },
      group: { name: 'Development Environments' },
      account: { id: '101234567890' },
    },
  ],
] as unknown) as NonEmptyArray<Synopses>;

export default Synopses;

// Success
