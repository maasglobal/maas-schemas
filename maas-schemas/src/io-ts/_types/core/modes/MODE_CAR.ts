/*

undefined
Schemas for MODE_CAR meta field

!!! AUTO GENERATED BY IOTSFJS REFRAIN FROM MANUAL EDITING !!!
See https://www.npmjs.com/package/io-ts-from-json-schema

*/

import * as t from 'io-ts';

import * as CarRental_7e2e_ from '../components/car-rental';

export const schemaId = 'https://schemas.maas.global/core/modes/MODE_CAR.json';

// MODE_CAR
// The default export. More information at the top.
export type MODE_CAR = t.Branded<CarRental_7e2e_.CarRental, MODE_CARBrand>;
export type MODE_CARC = t.BrandC<typeof CarRental_7e2e_.CarRental, MODE_CARBrand>;
export const MODE_CAR: MODE_CARC = t.brand(
  CarRental_7e2e_.CarRental,
  (x): x is t.Branded<CarRental_7e2e_.CarRental, MODE_CARBrand> => true,
  'MODE_CAR',
);
export type MODE_CARBrand = {
  readonly MODE_CAR: unique symbol;
};

export default MODE_CAR;

// Success
